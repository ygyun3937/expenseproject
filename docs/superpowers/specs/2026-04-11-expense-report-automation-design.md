# 영수증 경비정산서 자동화 시스템 설계

**날짜:** 2026-04-11  
**상태:** 확정

---

## 1. 개요

회사 직원(60명)이 영수증 이미지를 업로드하면 Gemini AI가 자동으로 항목을 인식하고, 검토 후 회사 양식에 맞게 경비정산서를 인쇄할 수 있는 로컬 웹 앱.

---

## 2. 사용자 워크플로우

```
영수증 이미지 업로드 (여러 장 동시)
        ↓
이미지 전처리 (자동, 백엔드)
        ↓
Gemini 2.0 Flash API 호출 (자동, 백엔드)
        ↓
결과 테이블 화면 표시 (가게명, 날짜, 항목, 금액)
        ↓
사용자 검토 및 수정 (인라인 편집)
        ↓
[인쇄] 버튼 클릭 → 브라우저 인쇄 페이지 → 회사 양식 PDF 출력
```

---

## 3. 아키텍처

### 플랫폼
- **런타임**: Python 3.10+ 로컬 서버 (Flask)
- **프론트엔드**: HTML/CSS/Vanilla JS (설치 불필요)
- **배포**: PyInstaller로 `.exe` 단일 파일 패키징 (Python 설치 불필요, ~50MB)
- **접속**: 실행 후 자동으로 브라우저 오픈 (`localhost:5000`)

### 기술 스택
| 역할 | 라이브러리 |
|------|-----------|
| 웹 서버 | Flask |
| 이미지 전처리 | Pillow |
| AI 인식 | google-generativeai (Gemini 2.0 Flash) |
| 패키징 | PyInstaller |
| 인쇄/PDF | 브라우저 기본 인쇄 기능 (CSS `@media print`) |

---

## 4. UI 레이아웃 (단일 페이지)

```
┌─────────────────────────────────────────────────┐
│              경비정산서 자동화                     │
├─────────────────────────────────────────────────┤
│  ┌───────────────────────────────────────────┐  │
│  │  📎 영수증을 여기에 드래그하거나 클릭해서 선택  │  │
│  │     JPG, PNG, PDF 지원 · 여러 파일 동시 가능  │  │
│  └───────────────────────────────────────────┘  │
│                                                 │
│  ┌──────┬──────────┬────────────┬────────┬───┐  │
│  │가게명 │  날짜    │   항목     │  금액  │ ✕ │  │
│  ├──────┼──────────┼────────────┼────────┼───┤  │
│  │[편집]│  [편집]  │   [편집]   │ [편집] │ ✕ │  │
│  └──────┴──────────┴────────────┴────────┴───┘  │
│                          합계: 00,000원          │
│                     [🖨️ 정산서 인쇄]             │
└─────────────────────────────────────────────────┘
```

---

## 5. 영수증 인식 전략

### 5-1. 이미지 전처리 (Pillow)
- 해상도 표준화: 장변 기준 최대 2048px로 리사이즈
- 밝기/대비 자동 보정 (`ImageEnhance`)
- EXIF 회전 정보 반영 자동 보정

### 5-2. 구조화 프롬프트 (코드에 고정, 사용자 개입 없음)
```
한국어 영수증 이미지입니다.
다음 항목을 JSON 형식으로 정확하게 추출해 주세요:
{
  "store_name": "가게명",
  "date": "YYYY-MM-DD",
  "items": [
    {"name": "품목명", "quantity": 수량, "unit_price": 단가, "amount": 금액}
  ],
  "subtotal": 소계,
  "tax": 부가세,
  "total": 합계
}
인식할 수 없는 항목은 null로 표시해 주세요.
반드시 JSON만 반환하고 다른 설명은 포함하지 마세요.
```

### 5-3. 결과 처리
- JSON 파싱 후 화면 테이블에 표시
- 파싱 실패 시 해당 셀을 빈칸으로 표시 (사용자 수동 입력 유도)
- 인식 오류는 사용자가 인라인 편집으로 수정

---

## 6. 인쇄 / PDF 출력

- 브라우저 기본 인쇄 기능 활용 (`window.print()`)
- CSS `@media print` 로 회사 양식에 맞는 레이아웃 적용
- 추후 회사 양식 제공 시 인쇄 CSS 수정으로 유연하게 대응

---

## 7. 규모 및 비용

| 항목 | 수치 |
|------|------|
| 직원 수 | 60명 |
| 정산 횟수 | 월 2회 |
| 월 예상 처리 건수 | ~120건 |
| Gemini 2.0 Flash 예상 비용 | 월 $1 미만 |

---

## 8. 설정 파일 (`.env`)

`.exe` 옆에 `.env` 파일을 함께 배포. API 키 관리자(구매자)만 수정.

```
GEMINI_API_KEY=your-api-key-here
```

모델명은 코드 내 폴백 목록으로 관리 (직원/관리자 개입 불필요, 아래 참고).

---

## 9. 모델 자동 대응 전략

경영부서 직원은 모델 변경을 인지하거나 개입할 필요 없음.

### 9-1. 폴백(Fallback) 모델 목록 (자동)
코드에 우선순위 모델 목록을 내장. 1순위 모델 호출 실패 시 자동으로 다음 모델 시도.

```
1순위: gemini-2.0-flash
2순위: gemini-1.5-flash
3순위: gemini-1.5-pro
```

직원은 아무것도 하지 않아도 자동으로 작동.

### 9-2. 전체 실패 시 친절한 안내 메시지
모든 모델이 실패할 경우 기술적 오류 대신 사용자 친화적 메시지 표시:

> "AI 서비스 연결에 문제가 발생했습니다.  
> API 키를 구매하신 담당자에게 문의해 주세요."

### 9-3. 사전 인지 경로
Google Cloud에서 모델 deprecated 안내 이메일을 API 키 구매 계정으로 자동 발송.  
→ API 키 관리자가 사전에 인지하고 폴백 목록을 업데이트 가능 (코드 수정 또는 재배포).

---

## 10. 배포

1. PyInstaller로 `.exe` 빌드
2. `.exe` + `.env` 파일 세트를 직원에게 공유 (USB, 사내망, 이메일 등)
3. 더블클릭 → 브라우저 자동 오픈 → 사용

---

## 11. 확장 가능성

- 현재: 개인 PC 로컬 실행
- 추후: 중앙 서버에 배포 시 코드 변경 없이 그대로 사용 가능 (Flask → Gunicorn/nginx)
- 양식 변경: 인쇄 CSS + 프롬프트 수정만으로 대응

---

## 12. 미결 사항

- 회사 경비정산서 양식 확인 후 인쇄 CSS 작성 필요
- API 키 관리 방식 확정 (`.env` 포함 배포 vs. 실행 시 입력)
